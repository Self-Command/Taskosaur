// Formatting instructions are language-agnostic (Mermaid/LaTeX syntax is universal).
// This section exists for future per-locale customization (e.g. RTL, CJK formatting).

import { PromptSection } from './types';

export const formatting: PromptSection = {
  key: 'formatting',
  render: () => '',
};
