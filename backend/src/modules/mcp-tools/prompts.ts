/**
 * DEPRECATED — System prompt construction has moved to:
 *   backend/src/modules/prompt/prompt-builder.service.ts
 *
 * This file is kept as a re-export shim for backward compatibility
 * during the transition. New code should use PromptBuilder directly.
 */
export { PromptBuilder } from '../prompt/prompt-builder.service';
